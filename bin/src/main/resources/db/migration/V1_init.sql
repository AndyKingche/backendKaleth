create table roles
(
   id             int not null auto_increment,
   rol                  varchar(20),
   descripcion          varchar(50),
   nombre               varchar(20),
   primary key (id)
);
